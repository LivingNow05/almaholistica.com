server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    absolute_redirect off;
    port_in_redirect off;

    # Gzip compression for better performance
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Cache static assets aggressively
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|webp|avif)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Sitemap and robots.txt
    location = /robots.txt {
        try_files $uri =404;
    }
    location = /sitemap-index.xml {
        try_files $uri =404;
    }
    location = /sitemap-0.xml {
        try_files $uri =404;
    }

    # 301 Permanent Redirects for legacy city URLs to biodescodificacion-[ciudad]
    # Preserves 100% SEO authority for legacy backlinks and search queries
    location ~* ^/(?!biodescodificacion|api|assets|_astro|images|favicons|robots\.txt|sitemap)([a-z0-9-]+)/?$ {
        return 301 /biodescodificacion-$1/;
    }

    # Main location — clean URLs for Astro SSG
    location / {
        try_files $uri $uri/ $uri.html =404;
    }

    # Custom 404
    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
